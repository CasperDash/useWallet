---
title: 'Getting Started'
description: 'Get started building with useWallet!'
---

import { Callout, Tabs, Tab } from 'nextra-theme-docs'

# Getting Started

## Manual setup

### Installation

Install useWallet and its ethers peer dependency.

{/* prettier-ignore-start */}
<Tabs items={['npm', 'pnpm', 'yarn']}>
  <Tab>
```bash
npm i useWallet
```
  </Tab>
  <Tab>
```bash
pnpm i useWallet
```
  </Tab>
  <Tab>
```bash
yarn add useWallet
```
  </Tab>
</Tabs>
{/* prettier-ignore-end */}

### Create a wagmi client

Next, create a wagmi `Client` instance using [`createClient`](/react/client), and pass the connectors.

```tsx {9-13}
import { 
  CasperDashConnector, 
  CasperSignerConnector, 
  CasperProvider, 
  createClient 
} from '@usedapp/core';


const client = createClient({
  connectors: [new CasperSignerConnector({}), new CasperDashConnector({})],
  autoConnect: true,
});
```

### Wrap app with CasperProvider

